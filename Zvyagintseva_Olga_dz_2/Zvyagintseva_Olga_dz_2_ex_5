# Создать вручную список, содержащий цены на товары (10–20 товаров)
costs = [57.8, 46.51, 97, 100.5, 58, 9.99, 84.1, 77.77, 45.65, 1.1]

print(f"{'Задание A':-^30}")
# A. Вывести на экран эти цены через запятую в одну строку, цена должна отображаться в виде  «5 руб 04 коп»).
result_costs = []
for i in range(len(costs)):
    price = str(f"{float(costs[i]):.2f}").split(".")
    result = f"{price[0]} руб {price[1]} коп"
    result_costs.append(result)


print(', '.join(result_costs))

print(f"{'Задание B':-^30}")
# B. Вывести цены, отсортированные по возрастанию, новый список не создавать.
# (доказать, что объект списка после сортировки остался тот же).
print(f'ID списка до сортировки: {id(costs)}')
costs.sort()
print(f'Список цен по возрастанию:\n{costs}\nID списка после сортировки: {id(costs)}')

print(f"{' Задание C ':-^30}")

# C. Создать новый список, содержащий те же цены, но отсортированные по убыванию.
costs_min = costs.copy()
costs_min.reverse()
print(f'Список цен по убыванию: {costs_min}')

print(f"{'Задание D':-^30}")

# D. Вывести цены пяти самых дорогих товаров. Сможете ли вывести цены этих товаров по возрастанию, написав минимум кода?
print(f'Самые высокие цены: {costs_min[:5]}')
